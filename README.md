# Clearveiw Windows — Feasibility Report

Interactive single-page report site (React 19 + Vite + Tailwind + Recharts).
Deployed on Cloudflare Pages from this repo.

## Build

```bash
npm install
npm run build   # outputs to dist/
```
